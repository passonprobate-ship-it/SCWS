# === Dependencies ===
node_modules/

# === Environment / secrets ===
.env
.env.*
!.env.example

# === Build artifacts ===
# Ignore all dist/ except daemon/dist/ (daemon source not on Pi)
dist/
!daemon/dist/

# === Runtime / logs ===
logs/
*.log
backups/

# === OS / editor ===
.DS_Store
*.swp
*.swo
*~

# === Claude Code local state ===
.claude/

# === Backup / old builds ===
*.bak
*.pre-funnel

# === Instance manifest (per-machine, not committed) ===
.spawn-instance.json

# === Projects — NEVER commit project source to this public repo ===
projects/

# === Backup token (sourced by offsite script) ===
scripts/.backup-token
